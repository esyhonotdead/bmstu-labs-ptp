#ifndef SORT_ALGORITHMS__
#define SORT_ALGORITHMS__

#include <stddef.h>

void insertion_sort_ind(double array[], size_t len);
void insertion_sort_mem(double array[], size_t len);
void insertion_sort_ptr(double array[], size_t len);

#endif
